# 1. 复制此文件并重命名为 .env.local
# 2. 填入你的真实密钥
# =================================================================

# -----------------------------------------------------------------
# 🤖 AI Model Configuration (DeepSeek)
# -----------------------------------------------------------------
# 获取地址: https://platform.deepseek.com/api_keys
# 用途: 用于核心的思维导图生成、扩写功能的底层 LLM 调用
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------
# 🗄️ Database Configuration (Supabase)
# -----------------------------------------------------------------
# 获取地址: Supabase Dashboard -> Project Settings -> API
# 用途: 连接 Supabase 项目实例
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# 用途: 客户端（前端）访问数据库的公钥
# 注意: 这个可以公开，配合 RLS (Row Level Security) 使用
NEXT_PUBLIC_SUPABASE_ANON_KEY=ey...

# ⚠️ 警告: 绝对不要在前端暴露此密钥！
# 用途: 服务端（API Routes / Webhooks）使用的超级管理员密钥
# 作用: 这里的代码需要绕过 RLS 权限（例如：Stripe 支付成功后强制修改用户积分）
SUPABASE_SERVICE_ROLE_KEY=ey...

# -----------------------------------------------------------------
# 💳 Payment Configuration (Stripe)
# -----------------------------------------------------------------
# 获取地址: Stripe Dashboard -> Developers -> API Keys
# 用途: 前端组件 (PricingModal) 初始化 Stripe Elements 时使用
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ⚠️ 警告: 仅限服务端使用
# 用途: 后端创建 Checkout Session、查询订阅状态
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 获取地址: Stripe Dashboard -> Developers -> Webhooks
# 本地开发: 安装 Stripe CLI 后运行 `stripe listen --forward-to localhost:3000/api/webhook`
# 用途: 验证 Stripe 发送的 Webhook 签名，防止伪造请求（处理充值、续费回调）
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------
# 🌐 App Configuration
# -----------------------------------------------------------------
# 用途: 支付成功/取消后的回调跳转地址
# 本地开发: http://localhost:3000
# 线上部署: https://your-project.vercel.app (记得去 Stripe后台 配置该域名)
NEXT_PUBLIC_APP_URL=http://localhost:3000
